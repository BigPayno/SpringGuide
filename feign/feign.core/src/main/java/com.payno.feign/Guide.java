package com.payno.feign;

/**
 * @author payno
 * @date 2020/5/25 11:17
 * @description
 */
public class Guide {
    /**
     *      Feign是一个声明式的Http客户端
     *      其主要特性为
     *      1.声明式（基于注解）
     *      2.工厂（clients[async、sync]）-> RestTemplate Factory
     *      3.契约(contracts)
     *      4.自动编解码(encoder,decoder) -> RestTemplate HttpMessageConverter
     *      5.指标(metrics)
     *      6.extras(hystrix,log...)
     */
}
